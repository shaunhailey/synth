import React, { Component } from 'react'
import Tone from 'tone'

// let synth = new Tone.Synth().chain(`${chain}`Tone.Master)
// var pingPong = new Tone.PingPongDelay(`${PPDdelayTime}`)
// var synth = new Tone.Synth().connect(pingPong)
// var master = '.toMaster()'

export default class Effect extends Component {
  constructor(props){
    super(props)
  }
    render(){

      let effectList = effects: {
        id: 1,
        name: pingPong,
        build: .PingPongDelay(`${PPDdelayTime}`))
      }
      {
        id: 2,
        name: vibrato,
        build: .Vibrato(`${VibFreq}`,`${VibDepth}`))
      }
    return (
      
    )
    }
}
